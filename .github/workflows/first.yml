name: First_workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!!!"
      - name: Print multiple lines
        run: |
          echo "Line 1"
          echo "Line 2"
          echo "Line 3"
      - name: Exit now
        run: echo "Exited"
